const colors = {
  // Base Backgrounds
  background: "#0A0E14",        // Deep navy-black (main background)
  backgroundSecondary: "#111827", // Dark blue-gray (side panels, timeline)

  // Primary Text
  textPrimary: "#E2E8F0",       // Light grayish-white (main text)
  textSecondary: "#94A3B8",     // Muted slate gray (secondary text)

  // Accents
  accentPrimary: "#3B82F6",     // Vivid Blue (primary buttons, highlights)
  accentSecondary: "#06B6D4",   // Cyan-Teal (sliders, secondary actions)

  // Highlights & Alerts
  highlight: "#FACC15",         // Golden Yellow (timeline playhead, warnings)
  error: "#EF4444",             // Red (errors, destructive actions)
  success: "#22C55E",           // Green (success confirmations)

  // Borders & Dividers
  border: "#1F2937",            // Subtle dark gray border
  divider: "#374151",           // Slightly lighter divider

  // Disabled / Muted
  disabled: "#6B7280",          // Muted gray (disabled buttons/text)
  overlay: "rgba(0,0,0,0.5)",   // Semi-transparent overlay (modals, dialogs)
};

export default colors;
